name: 'Hello World'
description: 'print a hello world msg'

inputs:
  msg:
    description: "msg"

runs:
  using: 'composite'
  steps:

    - name: print_msg
      shell: bash
      env:
        MSG: "${{ inputs.msg }}"
      run: echo "your message: $MSG"
